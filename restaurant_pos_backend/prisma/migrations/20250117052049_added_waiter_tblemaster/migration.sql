BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[tablemaster] ADD [WaiterName] VARCHAR(50);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

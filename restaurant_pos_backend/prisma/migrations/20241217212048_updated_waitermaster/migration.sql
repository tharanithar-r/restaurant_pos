BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[WaiterMaster] ALTER COLUMN [MPin] VARCHAR(100) NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
